#include <bits/stdc++.h>
using namespace std;

int main()
{
    double a = 2.2225;

    // C syntax for type casting
    int x = (int) a;

    // C++ syntax for type casting
    int y = int (a);

    cout << x << " " << y << endl;

    return 0;
}